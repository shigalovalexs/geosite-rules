# geosite-rules
Этот репозиторий автоматически раз в сутки (в 00:00 UTC) собирает правила маршрутизации из внешних источников, объединяет их в один список, убирает дубликаты и компилирует в бинарный формат `.srs`.
