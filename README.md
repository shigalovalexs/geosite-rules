# geosite-rules
Этот репозиторий автоматически раз в сутки (в 00:00 UTC) собирает правила маршрутизации и списки доменов из внешних источников, объединяет их в один оптимизированный блок, убирает дубликаты и компилирует в бинарный формат `.srs`.
